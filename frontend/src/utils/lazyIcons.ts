// src/utils/lazyIcons.ts

/**
 * Preload navigation icons for better performance
 */
export function preloadNavigationIcons() {
  // This function can be used to preload icons if needed
  // For now, it's a placeholder to prevent import errors
  return Promise.resolve();
}